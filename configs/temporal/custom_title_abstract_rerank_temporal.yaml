extends: ../base/custom_title_abstract_rerank.yaml
run_name: custom_title_abstract_rerank_temporal

temporal:
  enabled: true
  rerank_top_k: 200
  recency_weight: 0.25
  update_weight: 0.2
  foundation_weight: 0.15
  novelty_weight: 0.1
