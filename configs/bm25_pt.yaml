run_name: bm25_pt
pipeline: bm25_pt

dataset:
  backend: ir_datasets_longeval
  dataset_name: longeval-sci-2026/snapshot-1
  snapshot_id: snapshot-1
  qrels_variant: dctr

retrieval:
  type: bm25
  text_mode: title_abstract
  top_k: 100
  index_dir: indexes/bm25_pt

rerank:
  enabled: false

output:
  output_dir: outputs/bm25_pt

runtime:
  device: cpu
  batch_size: 16
  seed: 42
